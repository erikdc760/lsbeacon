// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { APIResource } from "../core/resource.mjs";
import { DefaultFlatPagination } from "../core/pagination.mjs";
import { path } from "../internal/utils/path.mjs";
export class UserAddresses extends APIResource {
    /**
     * Creates a user address.
     *
     * @example
     * ```ts
     * const userAddress = await client.userAddresses.create({
     *   business_name: "Toy-O'Kon",
     *   country_code: 'US',
     *   first_name: 'Alfred',
     *   last_name: 'Foster',
     *   locality: 'Austin',
     *   street_address: '600 Congress Avenue',
     * });
     * ```
     */
    create(body, options) {
        return this._client.post('/user_addresses', { body, ...options });
    }
    /**
     * Retrieves the details of an existing user address.
     *
     * @example
     * ```ts
     * const userAddress = await client.userAddresses.retrieve(
     *   'id',
     * );
     * ```
     */
    retrieve(id, options) {
        return this._client.get(path `/user_addresses/${id}`, options);
    }
    /**
     * Returns a list of your user addresses.
     *
     * @example
     * ```ts
     * // Automatically fetches more pages as needed.
     * for await (const userAddress of client.userAddresses.list()) {
     *   // ...
     * }
     * ```
     */
    list(query = {}, options) {
        return this._client.getAPIList('/user_addresses', (DefaultFlatPagination), {
            query,
            ...options,
        });
    }
}
//# sourceMappingURL=user-addresses.mjs.map
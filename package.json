{
    "name": "lsbeacon-root",
    "private": true,
    "version": "1.0.0",
    "engines": { "node": "20.x" },
    "scripts": {
      "build": "npm --prefix frontend ci || npm --prefix frontend install && npm --prefix frontend run build"
    }
  }
  